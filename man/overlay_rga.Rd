% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rga.R
\name{overlay_rga}
\alias{overlay_rga}
\title{Overlay Plot for Multiple RGA Models}
\usage{
overlay_rga(
  models,
  conf_bounds = TRUE,
  legend = TRUE,
  legend_pos = "bottomright",
  colors = NULL,
  log = FALSE,
  ...
)
}
\arguments{
\item{models}{A named or unnamed list of objects of class \code{rga}.
At least one model must be provided. If the list is named, those names
are used as legend labels; otherwise labels default to
\code{"Model 1"}, \code{"Model 2"}, etc.}

\item{conf_bounds}{Logical; draw confidence bounds for each model
(default: \code{TRUE}).}

\item{legend}{Logical; draw a legend (default: \code{TRUE}).}

\item{legend_pos}{Legend position keyword (default: \code{"bottomright"}).}

\item{colors}{Optional character vector of colors, one per model. If
\code{NULL} (default), \code{palette()} colors are cycled.}

\item{log}{Logical; use log-log axes (default: \code{FALSE}).}

\item{...}{Additional arguments passed to the initial \code{plot()} call
(e.g., \code{main}, \code{xlab}, \code{ylab}). Not forwarded to subsequent
\code{lines()} or \code{points()} calls.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Plots multiple fitted \code{rga} objects on a single set of axes, using
distinct colors per model. Observed data points, fitted lines, and optional
confidence bounds are drawn for every model. Models may have been fit to
different datasets.
}
\examples{
t1 <- c(100, 200, 300, 400, 500)
f1 <- c(1, 2, 1, 3, 2)
t2 <- c(150, 300, 450, 600, 750)
f2 <- c(2, 1, 3, 2, 4)
m1 <- rga(t1, f1)
m2 <- rga(t2, f2)
overlay_rga(list(System_A = m1, System_B = m2),
  main = "RGA Overlay", xlab = "Cumulative Time",
  ylab = "Cumulative Failures"
)
}
\seealso{
Other Reliability Growth Analysis: 
\code{\link{plot.rga}()},
\code{\link{plot.rga_predict}()},
\code{\link{predict_rga}()},
\code{\link{print.rga}()},
\code{\link{print.rga_predict}()},
\code{\link{rga}()}
}
\concept{Reliability Growth Analysis}
